# Session Context — 2026-05-24 Rebranding Session

## Project
- Monolith: neostronghold/monolith
- App (HA frontend fork): neostronghold/app (dev branch)
- Core (HA Core fork): neostronghold/core
- Web (brand website): neostronghold/web

## Completed (7 Phases)

### Phase 1: Core Palette Override
- Replaced HA 11-step color scales with neostronghold cosmic dark:
  - Primary: electric cyan (HSL 196 94% 48% = #06b6d4)
  - Neutral: cosmic dark (HSL 228°)
  - Green: emerald accent (HSL 160 84% 39% = #10b981)
- Updated darkColorStyles in `color.globals.ts`
- Replaced Roboto → Inter (body), Space Grotesk (headings), JetBrains Mono (code)
- Fixed bugs: `--ha-color-neutral-00` → `-05`, `--ha-color-90` → `--ha-color-neutral-90`

### Phase 2: Strip Theme System
- `apply_themes_on_element.ts` — simplified to always apply cosmic dark
- `themes-mixin.ts` — removed theme toggling, re-added minimal hassConnected that subscribes themes and updates hass.themes
- Removed `data/theme.ts`, `ha-pick-theme-row.ts` (empty shell)

### Phase 3: React + shadcn + Tailwind v4
- Installed: react, react-dom, tailwindcss, @tailwindcss/postcss, postcss-loader, cva, clsx, tailwind-merge, lucide-react, radix-ui/react-slot
- Created PostCSS config, Tailwind entry CSS with cosmic dark tokens + glass/glow/aurora utilities
- shadcn components: button (glow variant, pill shape), card, badge
- React mount helper (`react-root.ts`)
- Updated Rspack config for .tsx, postcss-loader

### Phase 4: UI Shell Components
- shadcn AppSidebar (collapsible icon mode, nav items, user avatar dropdown)
- shadcn SiteHeader (glass-strong, search, notifications)
- neostronghold-app-shell Lit component (not wired in — sidebar/header exist but not activated)

### Phase 5: Lovelace Glassmorphism
- Added --ha-card-* CSS variables for glassmorphism cards

### Phase 6: Rebrand Text & Logo
- 530+ "Home Assistant" → "neostronghold" in source, translations, gallery, tests
- Replaced HA house logo with neostronghold triangle SVG
- Updated HTML templates, manifests, package.json, README
- SVG only for all icons (no PNG/ICO)
- Custom element registration: @customElement("neostronghold") (was "home-assistant")

### Phase 7: Polish
- Launch screen: cosmic dark bg, white neostronghold logo
- Meta tags: theme-color #06b6d4, color-scheme: dark
- Added glow-pulse, gradient-rotate, aurora keyframes
- Font preload for Inter/Space Grotesk/JetBrains Mono

## Current State
- Core runs on tvpi (192.168.2.181) via Docker
- Frontend is served via bind mount: tvpi:~/neostronghold/app/hass_frontend → container's /root/.local/lib/python3.14/site-packages/hass_frontend
- Script for syncing local changes to tvpi: `app/scripts/sync-to-tvpi.sh`
- BUT the app is stuck on loading screen (neostronghold branded)
- NS: debug logs were added to trace the issue but user doesn't see them

## Debug Logs Added
- `entrypoints/core.ts`: `console.warn("NS: core.ts loaded")`, `console.warn("NS: creating connection...")`, `console.warn("NS: connection created")`, `console.warn("NS: auth resolved")`
- `state/connection-mixin.ts`: `console.warn("NS: initializeHass called")`, `console.warn("NS: initializeHass complete, calling hassConnected")`
- `state/themes-mixin.ts`: `console.warn("NS: themes-mixin firstUpdated")`, `console.warn("NS: theme applied")`, `console.warn("NS: themes-mixin hassConnected")`, `console.warn("NS: themes received")`
- `layouts/home-assistant.ts`: `console.warn("NS: waiting for init", {states, config, services, migration})`

## Current Issue
- The loading screen (neostronghold branded) shows forever
- "Lit is in dev mode" appears in console (from app.js)
- No NS: debug logs appear (user checked console)
- root cause UNKNOWN — could be auth hang, module import error, or JS execution failure

## Build Info
- Frontend build: `cd app && script/develop` (gulp + rspack watch mode)
- Node version: 22.22.3 (via nvm)
- Build output: `app/hass_frontend/`
- Quick dev sync: `bash app/scripts/sync-to-tvpi.sh` (rsyncs hass_frontend, restarts Core)

## Servers
- Core on tvpi: http://192.168.2.181:8123
- SSH: `ssh tvpi` (user: will, Raspberry Pi ARM64)
- Local build watch: `script/develop` in app/ (PID may need restart)
- Docker container: neostronghold-core

## Sync Script
```bash
# Sync local changes to tvpi and restart Core
rsync -avz --delete --exclude='*.map' --exclude='*.br' --exclude='*.gz' hass_frontend/ tvpi:~/neostronghold/app/hass_frontend/
ssh tvpi "docker restart neostronghold-core"
```

## To Continue
1. Debug why NS: logs don't appear (check if core.js actually loads)
2. The app.js loads (Lit dev mode), so dynamic imports work
3. Check if `getAuth()` hangs or if there's an import error in core.js
4. Fix the loading screen so the app initializes
5. Wire in the shadcn sidebar/header components
6. Set up automatic file syncing for faster dev cycle
